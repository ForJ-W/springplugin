# Roadmap

这个路线图用于说明项目接下来最值得投入的维护方向，方便贡献者选择任务，也方便评估项目申请开源支持时的真实需求。

## 近期

- 补充核心模块单元测试，覆盖插件信息、字符串工具、类加载路径和异常分支。
- 增强 README 的示例，加入从零编写插件的最小样例。
- 让 GitHub Actions 在 JDK 17 和 JDK 21 上执行 Maven 构建。
- 梳理 manager 模块接口的错误码和响应结构。

## 中期

- 增加插件生命周期事件，例如 beforeLoad、afterLoad、beforeUnload、afterUnload。
- 增加插件签名或校验机制，降低动态 jar 加载风险。
- 完善插件卸载后的 bean、线程、文件句柄和缓存清理。
- 增加集成测试，自动验证 server 加载 demo jar 后可以访问插件接口。

## 长期

- 提供更完整的插件开发 SDK 和模板。
- 支持插件依赖声明、版本冲突诊断和兼容性检查。
- 增加可选的管理 UI。
- 输出稳定版本并发布到 Maven Central。
