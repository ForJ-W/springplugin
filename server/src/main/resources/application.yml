server:
  port: 8000
spring:
  plugin:
    name: spring-plugin
    config:
      enable: true
      prefix: plugin-
      file-extension: yml
    intercept:
      white-list:
        - /pm/**
        - /v3/api-docs/**
        - /swagger-ui.html
        - /swagger-ui/**
        - /webjars/**
        - /favicon.ico
        - /error
      identity-mode: header
  servlet:
    multipart:
      max-file-size: 200MB
      max-request-size: 200MB
logging:
  level:
    org.springframework.web: trace
    web: trace
