server:
  port: 8000
spring:
  plugin:
    debug:
      resource-cache: true
    name: spring-plugin
    config:
      enable: true
      prefix: plugin-
      file-extension: yml
    intercept:
      white-list:
        - /pm/**
        - /v3/api-docs/**
        - /swagger-ui.html
        - /swagger-ui/**
        - /webjars/**
        - /favicon.ico
        - /error
  servlet:
    multipart:
      max-file-size: 200MB
      max-request-size: 200MB
